{
  "name": "health-ai",
  "version": "0.1.0",
  "private": true,
  "description": "HEALTH-ai (SRMNH Data Quality System) - developer scripts. The app itself is FastAPI + vanilla JS; Node is used only for JS syntax/import validation and tooling.",
  "scripts": {
    "check:js": "node scripts/check-js.js",
    "check:imports": "node scripts/validate-imports.js",
    "check": "npm run check:js && npm run check:imports",
    "lint:py": "ruff check app scripts tests",
    "lint:py:fix": "ruff check app scripts tests --fix",
    "test": "python -m pytest tests/ -q --no-header",
    "test:fast": "python -m pytest tests/ -q --no-header -x",
    "migrate": "alembic upgrade head",
    "migrate:verify": "alembic upgrade head && alembic downgrade base && alembic upgrade head",
    "dev": "python -m uvicorn app.main:app --reload"
  }
}
